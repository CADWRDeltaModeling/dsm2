#ifndef channel_ic_STORAGE_H__
#define channel_ic_STORAGE_H__
/**
WARNING: THIS FILE WAS AUTOMATICALLY GENERATED USING A SCRIPT AND A TEMPLATE  
DO NOT CHANGE THE CODE HERE. 
IF THE CODE IS INCORRECT, FIX THE TEMPLATE OR SCRIPT
IF YOU WANT TO ADD NEW ITEMS, ADD THEM TO THE SCRIPT INPUT FILE AND RUN IT AFRESH
*/ 
#define _CRT_SECURE_NO_DEPRECATE  // viz studio deprecation warnings
#include "hdf5.h"
#include "hdf5_hl.h"
#include "hdf_storage.h"
#include "HDFTableManager.h"
#include "TableDescription.h"
#include "TableItemFunctors.h"
#include "boost/tuple/tuple_comparison.hpp"
#include "boost/tuple/tuple_io.hpp"
#include<iostream>
#include<vector>
#include<algorithm>
#include<string.h>
#include<iostream>


using namespace std;
using namespace boost;

/** Structure representing input of type channel_ic.
    This class is autogenerated by a script given
    a description of the object
    \ingroup userdata
*/
class channel_ic
{
public:

  /** Data type channel_ic, default constructor */  
  typedef const boost::tuple<const int&,const std::string>  identifier_type;

  channel_ic() :
    chan_no(-901),
    stage(-901.0),
    flow(-901.0),
    used(true),
    layer(0)
  {
    fill_n(distance,8,'\0');
  };

  /** Construct a channel_ic with actual data values */
  channel_ic(const int & a_chan_no,const  char a_distance[8],const double & a_stage,const double & a_flow, bool a_used=true, int a_layer = 0) :
    chan_no(a_chan_no),
    stage(a_stage),
    flow(a_flow),
    used(a_used),
    layer(a_layer)
  {
    memcpy(distance,a_distance,8);  
  }
  
  /**Copy constructor) 
   */
  channel_ic (const channel_ic & other) :
    chan_no(other.chan_no),
    stage(other.stage),
    flow(other.flow),
    used(other.used),
    layer(other.layer)
  {
    memcpy(distance,other.distance,8);  
  }
  
  /** Identifier that distinguishes whether two entries are distinct */
  identifier_type identifier()  const
  {  
     return identifier_type( chan_no,distance );
  }
  
  void set_identifier(identifier_type identifier)
  {
     chan_no=identifier.get<0>();
      memcpy(distance,identifier.get<1>().c_str(),8);
  }
  
  /** Parent object class name.
      If this is a child item belonging to a parent, returns
      the name of the parent class. Otherwise returns the name
      of this class.
  */
  channel_ic::identifier_type parent_identifier()  const
  {
     return channel_ic::identifier_type( chan_no,distance );
  }

  /** Return the version/layer number of the parent object */ 
  int parent_version()  const
  {
    vector<channel_ic>& pbuf = HDFTableManager<channel_ic>::instance().buffer();
    channel_ic parent;
    parent.set_identifier(parent_identifier());
    vector<channel_ic>::iterator loc = lower_bound(pbuf.begin(),
                                                pbuf.end(),
                                                parent,
                                                identifier_compare<channel_ic>());
    bool found = (loc!=pbuf.end()) && loc->identifier() == parent.identifier();    
    if (found && loc->used){ return loc->layer; }
    else{ return -1; }
  }

  /** Return true if this layer of this object matches the layer of the parent object that will be use in the model.*/
  bool parent_valid()  const
  {
    return this->layer == parent_version();
  }

  /** Less-than operator based on the identifier plus (for parent objects) layer number*/  
  bool operator< (const channel_ic & other) const
  {
     
     if(this->identifier() != other.identifier())
	 {
		 return this->identifier() < other.identifier();
	 }
	 // todo: make this a policy
	 bool layerOutranks = (this->layer == 0 && other.layer != 0) ||
		                  (this->layer > other.layer && other.layer != 0);
     return layerOutranks;

  }

  /** Less-than operator based on the identifier plus (for parent objects) layer number*/  
  bool operator== (const channel_ic & other) const
  {
     return ((*this < other ) || (other < *this)) ? false : true;
  }
  
  /** Assignment that includes all the data plus the used and layer fields */
  channel_ic& operator=(const channel_ic& rhs)
  {
    this->chan_no=rhs.chan_no;
    strcpy(this->distance,rhs.distance);
    this->stage=rhs.stage;
    this->flow=rhs.flow;
    used = rhs.used;
    layer = rhs.layer;
    return *this;
  }

  /** Return the class name of this object (channel_ic) */
  string objectName() const
  { 
    return "channel_ic"; 
  }

  
  int chan_no;
  char distance[8];
  double stage;
  double flow;
  /** indicator that the entry is used (true if not marked deleted by user)*/
  bool used;  
  /** layer (version number) of this entry */
  int layer;
};

typedef HDFTableManager<channel_ic> channel_ic_table;

hid_t string_type(size_t n);

TableDescription channel_ic_table_description();

istream& operator>> (istream& stream, channel_ic & obj);
ostream& operator<<(ostream & stream, const channel_ic & obj);



////////// FORTRAN-LINKABLE API //////////
#define FCALL extern "C"


/**
  Clear the buffer, compatible with fortran
*/  
FCALL void channel_ic_clear_buffer_f();

/** query number of records being stored in buffer */
FCALL int channel_ic_buffer_size_f();


/** append to buffer, compatible with fortran, returns new size*/
FCALL void channel_ic_append_to_buffer_f(const int * a_chan_no,const  char a_distance[8],const double * a_stage,const double * a_flow, int * ierror, 
              const int distance_len);
  
/** both makes the table and writes the contents of the buffer to it */
FCALL void channel_ic_write_buffer_to_hdf5_f(const hid_t* file_id, int* ierror);

/** reads the table in from a file into the buffer*/
FCALL void channel_ic_read_buffer_from_hdf5_f(const hid_t* file_id, int* ierror);

/** query size information about the table in hdf5
*/
FCALL void channel_ic_number_rows_hdf5_f(const hid_t* file_id, hsize_t* nrecords, int* ierror);


/** get one row worth of information from the buffer */
FCALL void channel_ic_query_from_buffer_f(size_t* row, 
                        int * a_chan_no, char a_distance[8],double * a_stage,double * a_flow, int * ierror, 
              int distance_len);
/**
  prioritize buffer by layers, delete unused items and sort
  */
FCALL void channel_ic_prioritize_buffer_f(int* ierror);
/**
   write buffer to the given text file. File will be appended if exists and append flag is set to true.
   otherwise the file will be created or overwritten.
 */
FCALL void channel_ic_write_buffer_to_text_f(const char* file, const bool* append, int* ierror, int filelen);


#endif

