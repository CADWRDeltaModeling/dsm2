LIB=lib.a
OBJECTS2=${SOURCES2:.f=.o}
MEMBERS=${SOURCES2:.f=.o}

.KEEP_STATE:
.PRECIOUS: ${SOURCES2}

LOCAL_FLAG=-e -Nl99
FFLAGS= ${LOCAL_FLAG} -fast -dalign -O5 -depend

${LIB}: ${OBJECTS2}
	ar cq ${LIB} ${OBJECTS2}
