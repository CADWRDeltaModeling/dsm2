This empty directory is for user study output.

It is suggested that you set the output directory to this local folder. Avoid using deeper sub-folders such as "hydro" and "qual".

If you are using Condor, however, you must use a flat directory structure without this output subfolder.