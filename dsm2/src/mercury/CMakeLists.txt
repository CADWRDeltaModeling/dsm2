cmake_minimum_required(VERSION 3.21)
enable_language(CXX Fortran)
file(GLOB SOURCES *.f90)
add_library(mercury ${SOURCES})
target_include_directories(mercury PUBLIC ${HDF5_Fortran_INCLUDE_DIRS})
add_dependencies(mercury sediment_bed transport)
